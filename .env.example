# DVM service: private key. Generate: openssl rand -hex 32
DVM_SECRET_KEY=your_hex_private_key_here
RELAYS=wss://relay.brainrot.rehab

# Client build: set to DVM public key (from DVM logs after DVM starts)
VITE_DVM_PUBKEY=
